// since we are requiring the top level of faker, load all locales by default
var Faker = require('faker/lib');
var faker = new Faker({ locales: require('faker/lib/locales') });
module['exports'] = faker;